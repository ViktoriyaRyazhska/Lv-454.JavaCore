package lesson;

import java.util.Scanner;

import lesson.Task_week;
import lesson.Task_country.Continent;

public class practical_task3 {
	static Scanner sc = new Scanner(System.in);

	public static void main(String[] args) {
		int i = 0;
		final int J = 10;
		int Nombers[] = new int[J];
		for (i = 0; i < Nombers.length; i++) {
			System.out.println("Enter number");
			Nombers[i] = sc.nextInt();
		}
		int max = Nombers[0];
		int imax = 0;
		while (i < Nombers.length) {if (Nombers[i] > max) {	max = Nombers[i];imax = i;}i++;}
		System.out.println("Max= " + max + "in place " + (imax+1));

				int amountP = 0;
				for (int a : Nombers) {
					if (a > 0 && a <= 10) {amountP++;}
				}
				System.out.println("AmountP = " + amountP);
				int amountN = 0;
				for (int a : Nombers) {
					if (a < 0 && a <= 10) {amountN++;}
				}
				System.out.println("AmountN = " + amountN);

				if (amountN > amountP) {
					System.out.println("Negative is more");
				} else
					System.out.println("Positive is more");
			
			ArrOperations a = new ArrOperations(sc);
			ArrOperations b = new ArrOperations(sc);
			ArrOperations c = new ArrOperations(sc);
			ArrOperations d = new ArrOperations(sc);
			ArrOperations e = new ArrOperations(sc);
			ArrOperations[] arr = { a, b, c, d, e };{

			

			for (i = 0; i < arr.length; i++) {
				if (arr[i].getDNomber() == 2) {
					System.out.println(arr[i]);
					
					int tmp;
					int k;
					int l;
					for (i = 0; i < arr.length - 1; i++) {
						for (int j = i + 1; j > arr.length; j++) {
							if (arr[i].getSalary() > arr[j].getSalary()) {
								k = arr[i].getSalary();
								l = arr[j].getSalary();
								tmp = k;
								k = l;
								l = tmp;
							}
						}
					}
					for (i = 0; i < arr.length; i++) {
						System.out.println(arr[i]);
					}
				}
			}
		}
	}

}
//рограма глючить, максимальний елемент рахується погано, чомусь завжди кидає 1 в списку, як максимальний. кількість позитивних негативних нормально працює через раз, часом буває похибка в 1-2 одиниці. сортування теж працює не найліпшим чином, при тому, я звіряв код із методичкою, і там все було добре.